@model IEnumerable<Sistema.Web.Data.Entities.Company_Classification>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CompanyClass_Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CompanyClass_Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CompanyClass_Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CompanyClass_Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CompanyClass_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
