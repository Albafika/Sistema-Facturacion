@model IEnumerable<Sistema.Web.Data.Entities.ArticleClassification>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Codigo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name_ArtC)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Codigo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name_ArtC)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ArtC_Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ArtC_Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ArtC_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
