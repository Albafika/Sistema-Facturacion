@model IEnumerable<Sistema.Web.Data.Entities.Moneda>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name_moneda)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tasa_moneda)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nmoneda)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name_moneda)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tasa_moneda)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nmoneda)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Moneda_Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Moneda_Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Moneda_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
